# Runtime dependencies (mirrors [project].dependencies in pyproject.toml)
sympy
numpy
scipy
ipython
ipywidgets
plotly
anywidget
traitlets

# Testing infrastructure dependencies (used by .environment scripts)
pytest>=8
pytest-cov>=5
