# MathLive identifier-system notebook bug reports

Generated from analysis of:

- `examples/MathLive_identifier_system_showcase.ipynb`
- the related semantic-math source files under `src/gu_toolkit/identifiers/` and `src/gu_toolkit/mathlive/`
- the relevant documentation and regression tests under `docs/` and `tests/`

Included reports:

1. Issue 035 — semantic-math docstrings are placeholder-heavy
2. Issue 036 — docstring tests do not cover semantic-math subpackages
3. Issue 037 — notebook is not markdown-first and still contains author-only BUG cells
4. Issue 038 — low-level transport manifest is introduced too early and without a schema
5. Issue 039 — function-registration contract is underexplained
6. Issue 040 — `IdentifierInput` demo conflates teaching with hidden assertions
7. Issue 041 — semantic-math cross-references point to generic or missing navigation targets
8. Issue 042 — toolkit `symbol()` helper is underdocumented relative to SymPy
